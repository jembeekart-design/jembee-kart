"use client";

import { createProduct } from "@/shared/modules/product/productService";

export default function SellerProducts() {
  const add = () => {
    createProduct({
      title: "Seller Product",
      basePrice: 400,
    });
  };

  return (
    <div>
      <h1>My Products</h1>

      <button onClick={add}>Add Product</button>
    </div>
  );
}